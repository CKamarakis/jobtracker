# Secrets — never commit
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Data — generated, not tracked
data/jobs.jsonl
data/status.json
data/*.bak

# Eval run artifacts (raw logs from each eval run)
evals/runs/

# API dev-server runtime PID (written by api/serve.ps1)
api/.server.pid

# IDE / OS noise
.DS_Store
Thumbs.db
.vscode/
.idea/

# Personal data — kept LOCAL ONLY for the public repo (loosen manually later).
# Strict by design: salary anchors, CV, named-company verdicts, drafted letters.
profile/
output/
data/dossiers/
evals/labeled/
evals/labeled.json
.claude/settings.local.json
